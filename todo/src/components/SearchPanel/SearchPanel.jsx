import React from 'react';

import './SearchPanel.css';

const SearchPanel = () => {

  return <input placeholder={'search'} type="text" className='form-control search-panel'/>;
};
export default SearchPanel;
